# TenSafe — GPU-accelerated HE-LoRA inference server
# Install: pip install -r requirements.txt
# Requires: CUDA 12.8+ GPU, Python 3.12+

# ── Core ML ───────────────────────────────────────────────────
torch>=2.6.0
transformers>=4.45.0
accelerate>=1.0.0
safetensors>=0.4.0
tokenizers>=0.20.0

# ── Homomorphic Encryption ────────────────────────────────────
cukks>=0.1.2                  # CuKKS CKKS wrapper (install cukks-cu128 for GPU)
# cukks-cu128>=0.1.2          # GPU backend — install separately for your CUDA version
Pyfhel>=3.5.0                 # CPU fallback CKKS backend
openfhe>=1.4.0                # OpenFHE bindings (used by CuKKS)
tenseal>=0.3.16               # TenSEAL (optional, for benchmarking)

# ── Server ────────────────────────────────────────────────────
fastapi>=0.100.0
uvicorn>=0.30.0
websockets>=12.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# ── Utilities ─────────────────────────────────────────────────
numpy>=2.0.0
psutil>=5.9.0
requests>=2.31.0
tqdm>=4.65.0
PyYAML>=6.0
regex>=2024.0.0
python-dotenv>=1.0.0
